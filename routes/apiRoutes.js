const express = require('express');
const router = express.Router();
const noteId = require('uuid');
const fs = require ("fs");

router.get('/api/notes', (req, res) => {
    
res.json();
});

module.exports = router;